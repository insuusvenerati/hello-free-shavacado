import { Text } from "@nextui-org/react";

export default function PrivacyPage() {
  return (
    <div>
      <Text h4>
        This app stores a single cookie. It&apos;s used to store the token from
        the Hello Fresh API which is required by Hello Fresh to query for
        recipes. No personal data is transmitted to or from this app.
      </Text>
    </div>
  );
}
